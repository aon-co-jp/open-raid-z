# このリポジトリの既存コードは「1行を比較的長く使う」詰めたスタイルで
# 書かれている(既定のmax_width=100では大量の差分が出る)。CIの
# `cargo fmt --check`と実際のコードスタイルを一致させるための設定。
max_width = 120
# メソッドチェーン・関数呼び出し等を可能な限り1行へ収める(既存スタイル準拠)。
use_small_heuristics = "Max"
